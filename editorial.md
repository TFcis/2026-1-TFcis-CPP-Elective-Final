# 114下高一選修期末考題解

:::warning
注意：題解所附之參考程式碼並非標準解答的一部份，為表達想法用，故可能存在錯漏，僅供參考。
:::

# A - 忘卻疑惑 [TOJ 1122](https://toj.tfcis.org/oj/contests/47/pro/1122/)

經典的幾 A 幾 B 問題。

觀察問題，結果分成 $3$ 種狀態：

- `Impossible`：不存在任何 $6$ 位數數字同時符合 $n$ 次問答
- `Ambiguous`：存在至少 $2$ 組以上 $6$ 位數數字同時符合 $n$ 次問答
- 唯一解：存在恰 $1$ 組 $6$ 位數數字同時符合 $n$ 次問答

因此，基本策略為窮舉所有 $6$ 位數數字，個別作為答案去檢查是否符合這 $n$ 次問答。
記錄符合的數字個數，分為 $0$、$1$、$\geq 2$ 三種 case 來回答。
剩下就是如何實作給兩數字 $X, Y$ 求 A 與 B 的數量。

實作參考如下

```cpp=
int ans;
int cnt = 0;
// 窮舉所有可能答案 i，一旦找到 2 組以上就能確定多重解，可提早結束
for (i=0; i<=999999&&cnt<2; i++)
{
    // 套用迴圈求「是否全符合 / 至少 1 不符合」的 pattern
    bool is_fit = true;
    for (j=0; j<n; j++)
    {
        // 設詢問存在 query[] 回答存在 ary[], bry[]
        int a, b;
        // 計算 i 和 query[j] 的 A, B 數量，存在 a 和 b 中
        calculate(i, query[j], a, b);
        if (a != ary[j] || b != bry[j])
        {
            is_fit = false;
            break;
        }
    }
    if (is_fit)
    {
        ans = i;
        ++cnt;
    }
}
// 分 3 種 case
if (cnt == 0)  // 矛盾
{
    // 印出 Impossible
}
else if (cnt == 1)  // 唯一解
{
    // 印出 ans
}
else
{
    // 印出 Ambiguous
}
```

注意印出 ans 時，若以整數儲存，必須補足開頭 $0$ 讓它成為 $6$ 位數數字。

至於問答，已知 A 優先，可先掃過求存在多少 $i$ 滿足 $X_i=Y_i$，並標記為已配對。
接下來可對所有 $X$ 中，找出所有未標記配對者，個別於 $Y$ 尋找相等且未標記者是否存在。

```cpp=
// 假設猜測與答案皆拆好放在 x[] 與 y[] 中
bool mark_a[6] = {};
bool mark_b[6] = {};
// 先求 A
int a = 0;
for (i=0; i<6; i++)
{
    if (x[i] == y[i])
    {
        ++a;
        mark_a[i] = true;
        mark_b[i] = true;
    }
}
// 再求 B
int b = 0;
for (i=0; i<6; i++)
{
    if (!mark_a[i])  // 僅對未標記者尋找配對
    {
        for (j=0; j<6; j++)
        {
            if (!mark_b[j] && x[i] == y[j])
            {
                ++b;
                mark_a[i] = true;
                mark_b[j] = true;
                break;  // 以防多重標記
            }
        }
    }
}
```

窮舉範圍 $n\times 10^6$ 每組約 $6\times 6=36$ 次計算，約 $10^{10}$ 計算量。
但是經過第一次問答其實就會刷掉個大半，所以中途 break 的話不會那麼可怕。

## 改進

透過 bucket 能夠進一步縮減計算量。記錄每個數字未使用次數，取最小值即為最大配對。

```cpp=
// 假設猜測與答案皆拆好放在 x[] 與 y[] 中
int cnt_a[10] = {};
int cnt_b[10] = {};
// 先求 A
int a = 0;
for (i=0; i<6; i++)
{
    if (x[i] == y[i])
    {
        ++a;
    }
    else  // 不在 A 中，就列入 B 候選
    {
        ++cnt_a[x[i]];
        ++cnt_b[y[i]];
    }
}
// 再求 B
int b = 0;
for (i=0; i<10; i++)  // 對每種數字，取兩邊出現較少者，即為最大配對數
{
    b += min(cnt_a[i], cnt_b[i]);
}
```

原本每組要 $36$ 左右計算量，壓到約 $10$ 上下。

### subtask 2

既然 $n = 1$ 幾乎必定 `Ambiguous`，除了一個例外：`6A0B`。
只須判斷是否為 `6A0B`，$15$ 分輕鬆到手。

### subtask 3

引導式子題。既然六位相同，只有 $10$ 種情形。很容易想到窮舉。
即使要做邏輯推論，在過濾時也會簡單很多。

### subtask 4

前兩位數固定為 $0$ 所以實質上只有 $4$ 位，問題較為簡化。
同時作為一種提示：如果輸出整數未加開頭 $0$ 這組理論上會全錯。

### subtask 5

僅為那些有找出如何求解，或者真的邏輯推論成功者，獻上 $15$ 分。
不知道如何判斷矛盾或多重解，至少能拿下這組。

### subtask 6

沒有矛盾，所以如果推論正確…
老實說只是預防性的擺設，至少我不認為有人能夠拿下這組子題，卻拿不到滿分。


# B - 醉言醉語 [TOJ 1123](https://toj.tfcis.org/oj/contests/47/pro/1123/)

單純的字串插入 / 刪除字元，上課提過的例題。

對字串插入或刪除字元，會導致該位置往右所有字元的 index 產生變動。
有多少 index 發生變動，代表有多少元素需要搬家。

若不斷對每個位置進行插入或刪除，則搬家次數為 $0+1+2+\ldots+(n-1)$
大約為 $n^2$ 的量級，以 $n$ 最大 $10^6$ 來說， $10^{12}$ 的計算量過大。
以每秒 $10^8$ 次計算來看，需要 $10000$ 秒，約 $3$ 小時，並不現實。

這些計算量即便使用內建的 `insert()` 或 `erase()` 也無法迴避。

依上課所說，若以建立新字串，用搬運至字串尾的方式，就能夠避免。
因為字串尾後面沒有其它字元，不會產生多餘的搬運。
這樣只需 $n$ 的計算量，大約 $10^6$。

```cpp=
// 這裡假設 A 用 bucket 形式保存於 del[] 而 B 則以 bucket 形式保存於 ins[]
string ans;
for (i=0; i<s.size(); i++)
{
    if (del[s[i]])
    {
        // 不用做任何事
    }
    else if (ins[s[i]])
    {
        ans += 'Q';
        ans += s[i];
        ans += 'Q';
    }
    else
    {
        ans += s[i];
    }
}
// ans 即為所求
```

即使不使用 bucket，每次從 $A$ 或 $B$ 重新搜尋，
由於 $A$ 與 $B$ 不重複的特性，長度不超過 $126-33+1=94$。
$9.4\times 10^7$ 仍在可接受範圍內。

### subtask 2, 3

$S$ 極端小，且只包含其中一邊。計算邏輯正確即可。

### subtask 4, 5

$S$ 可以極端大，但只包含插入或刪除其中一種。不會做另一種時，可以從這裡取得分數。
觀察這 $2$ 組的對錯，也能方便特定出錯的是哪一種 case。

### subtask 6

如果你確實會做插入與刪除，但無法做得很有效率。
那麼對 $S$ 找出正確的循環節，對循環節計算正確答案，再將其反覆循環次數即可。
反覆過程或許你會發現，反覆串在字串尾這件事其實很快。

至少反覆印很快。也許你會想到，邊掃 $S$ 邊印也行。

### subtask 7

退化到 $n^2$ 大概也還能過。計算邏輯完全正確的獎勵分。

### 整體而言

即使你使用 `insert` 和 `erase`，或者自力實作了它們。
理論上你仍然可以得到 $10+10+20=40$ 分。

如果你還是沒能發現如何處理，再多觀察一下、找個循環，可以拿到 $60$ 分。


# C - 乾史萊姆 [TOJ 1124](https://toj.tfcis.org/oj/contests/47/pro/1124/)

這題需要二維陣列來管理每天的地圖狀態。
對每隻史萊姆存在的每一格，往外擴張。

為此，你必須能夠分辨每一格當前屬於哪一隻史萊姆，以及是否正處於戰爭狀態。
你的每隻史萊姆必須有獨一無二的 ID，並且能夠分辨某格子是否被史萊姆所佔據。
每一格會需要記錄複數資訊，這會需要複數個二維陣列，或者三維陣列。

史萊姆的擴張在同一天伸向同一格時，會引發戰爭狀態。
但同一天史萊姆也必有先後順序，無法同時計算。
你必須能夠認出哪些格子是當日才擴張的，而哪些是昨日就已被佔據。

這點較佳的做法是，當天先只做出佔領宣告，不直接佔領。
佔領宣告衝突時，轉入戰爭狀態。
確認完所有史萊姆的擴張後，再將宣告轉為佔領事實。

由於宣告還未成為事實，能較容易找出衝突、或者即時變更。

另，周圍八格建議用陣列保存，較利於迴圈窮舉。

```cpp=
// 注意：由於 code 較長較複雜，為免失焦，這份 code 並未對障礙物進行判斷
// 宣告常數以便用整數記錄格子狀態
const int NO_SLIME = 0;
const int DX[] = {0, 1, 0, -1, 1, 1, -1, -1};
const int DY[] = {1, 0, -1, 0, 1, -1, 1, -1};
const int WAR = -1;
for (k=0; k<n*m; k++)
{
    int claim[105][105] = {};
    for (i=0; i<n; i++)
    {
        for (j=0; j<m; j++)
        {
            // 用 map 會撞內建，我習慣用 board
            if (board[i][j] != NO_SLIME && !war[i][j])
            {
                for (t=0; t<8; t++)
                {
                    int nx = i + DX[t];
                    int ny = j + DY[t];
                    // 確認未出界
                    if (nx >= 0 && nx < n && ny >= 0 && ny < m)
                    {
                        // 如果未佔領、未戰爭，就嘗試宣稱佔領
                        if (board[nx][ny] == NO_SLIME && !war[nx][ny])
                        {
                            // 沒事，自己人
                            if (claim[nx][ny] == board[i][j])
                            {
                                continue;
                            }
                            // 如果衝突，就戰爭
                            if (claim[nx][ny] != NO_SLIME)
                            {
                                claim[nx][ny] = WAR;
                            }
                            // 沒衝突就宣稱佔領
                            else
                            {
                                claim[nx][ny] = board[i][j];
                            }
                        }
                    }
                }
            }
        }
    }
    // 看過所有史萊姆後，宣稱已不會再有變動，將其轉為事實
    for (i=0; i<n; i++)
    {
        for (j=0; j<m; j++)
        {
            if (claim[i][j] == WAR)
            {
                war[i][j] = true;
            }
            else if (claim[i][j] != NO_SLIME)
            {
                board[i][j] = claim[i][j];
            }
        }
    }
}
```

## 常見錯誤之一

注意雖然從地圖邊界到邊界只需約 $H+W$ 步，但考慮障礙物可以到接近 $H\times W$ 步：

```
5 5
S....
####.
.....
.####
.....
```

保險起見可將最大天數設為 $H\times W$，
或者判斷若當日未能進行任何擴張，則為穩定狀態。

## 常見錯誤之二

性質上戰爭狀態會很大程度阻礙史萊姆的行軍，
故若先以 BFS 等方式個別計算每隻史萊姆需要幾日抵達每一格，
或者對史萊姆個別擴張，遇到日數較大的地盤則搶佔、日數相同則戰爭的話，
會難以處理史萊姆被阻斷導致的後果：無法繼續前行，或進軍日數受到影響。

### 例 1

```
3 3
##S
...
##S
```

以上例子中間最左格對兩隻史萊姆都是 $2$ 日抵達，應視為戰爭狀態；
事實上第 $1$ 日兩隻史萊姆便會撞在一塊，沒有任何一隻史萊姆能夠抵達並佔領中間最左格。
因此它會是空格，不會被佔領，也不會進入戰爭狀態。

```
Day0   Day1
##1    ##1
... => .WW
##2    ##2
```

### 例 2

```
4 3
...
.#.
..S
..S
```

第 $3$ 排最左對 $2$ 隻史萊姆而言都是 $2$ 天抵達，應為戰爭狀態；
實際上只有史萊姆 $1$ 能夠花非常多天抵達。

```
Day0    Day1             Day100
...     ...              111
.#.  => .#1  => (...) => 1#1
..1     .W1              1W1
..2     .W2              1W2
```

### subtask 2

史萊姆只有 $1$ 隻，無須處理佔領先後，無須處理戰爭地帶。
只要能夠正確計算擴張即可。

注意障礙物的存在，可能令某些空格無法抵達，因此單純計算空格數是不足夠的。

### subtask 3

地圖可視為一維數線，且無須處理障礙物。
極端來說，也不需要處理擴張。

每隻史萊姆只可能和左右相鄰史萊姆產生接觸。
只需由左到右找出所有史萊姆的位置，
利用距離的奇偶性，即可判斷擴張狀態，以及是否進入戰爭狀態。

### subtask 4

不存在障礙物。
判斷的難易度下降，進入穩定狀態所需日數下降。

另外，這組全對但其它出錯，就表示戰爭狀態有處理好，但障礙物沒有。

### subtask 5

所有史萊姆間完全隔離，表示只要能夠計算各自可抵達區域的大小即可。
不用處理戰爭，可以獨立計算每隻史萊姆。
相當於 floodfill 的經典問題。


# D - 下場一定 [TOJ 1125](https://toj.tfcis.org/oj/contests/47/pro/1125/)

這題算是相當困難的一題。
沒有一些演算法或資料結構相關知識，可能不是那麼容易找出正確且足夠有效率的做法。

題意是尋找最短區間，使得每個目標字元的出現次數皆滿足各自標準。
最直接的方法是窮舉所有區間，對每個區間檢查字元出現次數是否達標。

區間的數量為 $n^2$，檢查區間字元出現次數最糟情形需要比對 $n$ 個字，整體為 $n^3$。
以這題 $n$ 最大 $10^6$ 來看，高達 $10^{18}$ 的計算量完全不可行。

不過儘管滿分解相當困難，這題仍有許多相當簡單的子任務可以拿。

### subtask 2

由於 $T$ 僅含 $1$ 種字元，且出現次數與 $S$ 相等，那麼不需考慮要拿哪些了。
$T$ 出現的字元每個都必須拿，其它都不需要。

找到該字元第 $1$ 次出現的位置，以及最後 $1$ 次出現的位置，此區間即為最佳。

此方法只需 $n$ 的計算量，掃過 $S$ 一遍即可。
就算掃 $2$ 次分開求最初與最後也沒問題。

### subtask 3

由於 $T$ 僅包含 $2$ 個字元，可分為 $2$ 種情形討論：

- 兩字元相等：求該字元每次出現與下次出現離多遠，取最小值即為所求。

假設該字元於 $S$ 共出現 $k$ 次，依序為 $A_1, A_2, \ldots, A_k$。
則所求為 $\min(A_i-A_{i-1}+1)$。其中 $i$ 為 $2$ 到 $k$。

- 兩字元不相等：每個字元都和另一個字元上次出現位置湊對即可。

例如 $T$ 為 `xy` 的話，每次找到 `x` 就去翻上次找到 `y` 的地方，
這段區間即滿足條件；每次找到 `y` 就反過來翻上次找到 `x` 的地方。

如此只要不斷維護最後一次找到 `x` 與 `y` 的地方，即可求解。
畢竟只要不是最後一次，就必定離得更遠，沒有意義。

以上兩個做法都只需要 $n$ 的計算量，能夠輕易通過測試。

### subtask 4

$S$ 與 $T$ 皆遞增。
由於必定有解的關係，只需考慮 $T_1$ 和 $T_m$ 的最佳出現位置即可。
其它都一定被包在中間，不影響答案。

因此，找出 $T_1$ 和 $T_m$ 各自出現次數，
從 $S$ 找到 $T_1$ 最後出現位置往回算，
從 $S$ 找到 $T_m$ 初次出現位置往後算，
即可找到最小滿足區間。

尋找位置可直接掃過 $S$，總計算量為 $O(n+m)$。
或者可以用二分搜尋做到 $O(\log n+\log m)$。

只要注意 $T_1=T_m$ 的情形即可。此時答案明顯為 $m$，但照上述做法會算錯。

### subtask 5

如果你注意到窮舉區間的過程中，當從 $(l, r)$ 推進至 $(l, r+1)$ 時，
只比 $(l, r)$ 時多了 $1$ 個元素。
若維護原先 $(l, r)$ 區間各字元出現次數，應能輕易轉換至 $(l, r+1)$。

如此一來，原先暴力窮舉的計算量將降至 $O(n^2)$，能夠通過這組測試資料。

## 官解

從個別字元出現次數必須滿足要求，可觀察出若 $T$ 中某字元 `c` 出現 $3$ 次，
那麼滿足此出現次數，最低限度為 $S$ 中任找 $3$ 個 `c` 出現位置，
取最小區間覆蓋這 $3$ 個位置。

若要區間盡量小，那麼選擇的 $3$ 個位置，顯然也應該盡量靠近。
也就是說，若以 $P_1, P_2, \ldots, P_k$ 代表 `c` 在 $S$ 中所有出現位置，
且滿足 $P_1 < P_2 < \ldots < P_k$，則最佳選擇應為
- $P_1, P_2, P_3$
- $P_2, P_3, P_4$
- $\vdots$
- $P_{k-2}, P_{k-1}, P_k$

其中之一。

從上面觀察可發現，對每個字元各自儲存排序過的出現位置，在這個想法上是有利的。
結構上會儲存成二維陣列。

```cpp=
for (i=0; i<n; i++)
{
    int c = s[i];
    appear[c][ cnt[c] ] = i;
    ++cnt[c];
}
```

接著考慮多個字元的情況，設另個字元 `d` 在 $T$ 出現 $4$ 次，
在 $S$ 中的出現位置以 $Q$ 表達，那麼例如選擇
- $P_i, P_{i+1}, P_{i+2}$
- $Q_j, Q_{j+1}, Q_{j+2}, Q_{j+3}$

時，同時覆蓋它們的最小區間 $(l, r)$ 為 
- $l = \min(P_i, Q_j)$
- $r = \max(P_{i+2}, Q_{j+3}))$

以上可簡單推廣至任意多字元。

下個問題會是如何對每個字元選擇適當的起點，使它們能夠盡量靠近。

考慮大家都從最左邊開始，即 $P_1, Q_1, \ldots$。
很簡單可以證明這會是 $l$ 最小化的可行區間。
而 $r$ 會是對應 $l$ 時的最小值，這也非常明顯。

接下來的想法很單純，像窮舉法一樣，找次小的 $l$。
很明顯地必須找出左界恰為 $l$ 者往右推，$l$ 才能獲得前進。
對所有字元找出左界為 $l$ 者，例如 $P_1, P_2, P_3$，
往右推一格就變成 $P_2, P_3, P_4$。

每次右推重新計算新的 $l$ 和 $r$。
可以證明新的 $l$ 會是次小的可行左界，而 $r$ 會是對應至該 $l$ 的最小右界。

由上步驟反覆推進至要推進的字元字數已經不足，無法再推進為止。
理論上可窮舉過所有可能的 $l$，並對該 $l$ 有效率地找到最小的對應 $r$。
可證明最佳解必定存在於這樣的窮舉過程中。

總推進次數最多為 $S$ 長度 $n$，每次推進需要窮舉所有字元約 $100$ 種。
計算量約 $100n = 10^8$ 粗估 $1$ 秒能夠跑完。

```cpp=
int ans = n;
while (true)
{
    int l = n + 1;
    int r = 0;
    int nxt;
    // 找最小區間使能完整覆蓋當前每個字元所選區間
    for (i=33; i<=126; i++)
    {
        // 先確認是否出現於 T，沒出現就無視
        if (is_in_t[i])
        {
            // 更新左右界，同時記錄要推進的字元（左端最小者）
            if (appear[i][ lp[i] ] < l)
            {
                l = appear[i][ lp[i] ];
                nxt = i;
            }
            r = max(r, appear[i][ rp[i] ]);
        }
    }
    // 更新答案
    ans = min(ans, r-l+1);
    // 嘗試推進，推不動就結束
    ++lp[nxt];
    ++rp[nxt];
    if (rp[nxt] >= cnt[nxt])
    {
        break;
    }
}
```

### 另解一

從官解可觀察出，當 $l$ 推進時，$r$ 是不會變小的，最多維持不變。
因為對 $l$ 而言，$r$ 是最小滿足條件的右界，
也就是說 $(l, r-1)$ 是不滿足條件的區間。
那麼很明顯地 $(l+1, r-1)$ 也必定不滿足條件，無須考慮。

由上可以證明，最小右界 $r$ 對 $l$ 是有單調性的。
若 $l_i<l_j$ 則 $r_i\leq r_j$。

維護當前區間每個字元出現次數，對 $l$ 找到最小滿足右界 $r$。
推進至 $l+1$ 時，從上次的 $r$ 繼續向下找最小滿足右界。

每次 $l, r$ 推進時，只需 $1$ 次計算就能從 bucket 維護字元出現次數，
只需 $1$ 次計算就能維護目前滿足條件的字元種類數。
而 $r$ 永遠只會增加，故對單一 $l$ 最大推進次數雖為 $n$，
過程將所有 $l$ 的右界最大推進次數總和起來，也無法超過 $n$。

得到計算量為 $n$ 的做法。

附帶一提，這個技巧叫作 two-pointer。
是善用單調性的技巧之一。

```cpp=
int fit = 0;
int ans = n;
// 窮舉左界，右界不後退
for (l=0, r=0; l<n; l++)
{
    // 推進右界到符合為止，維護符合的種類數來避免重複確認所有字元
    for (; r<n&&fit<total; r++)
    {
        ++cnt[s[r]];
        if (cnt[s[r]] == need[s[r]])
        {
            ++fit;
        }
    }
    // 更新答案
    if (fit >= total)
    {
        ans = min(ans, r-l);
    }
    // 維護 l 推進時的字元出現數
    if (cnt[s[l]] == need[s[l]])
    {
        --fit;
    }
    --cnt[s[l]];
}
```

### 另解二 (80 分)

觀察可發現，固定 $l$ 時，若 $r$ 向右，字元數只會更多；
因此必存在一個分界線 $k$，使得 $(l, k-1)$ 為不符合條件區間，
而 $(l, k)$ 為符合條件的區間。

當一個區間不符合條件，則縮小其左右界亦不符合條件；
當一個區間符合條件，則擴大其左右界亦符合條件。

從以上性質可知 $l\leq r\leq k-1$ 時 $(l, r)$ 全部不行；
$k\leq r\leq n$ 時 $(l, r)$ 全都可以。

故 $l$ 固定不變時，$r$ 對到條件符不符合，是具備單調性的。
設 $f(r) = 0$ 代表 $(l, r)$ 不符合條件，$f(r) = 1$ 代表 $(l, r)$ 符合條件，
則 $i<j$ 符合 $f(i)\leq f(j)$ 故 $f$ 具單調性。

因此，分界點 $k$ 可透過二分搜尋在 $\log n$ 次搜尋內找出來。
每次搜尋必須有效率地回答該區間是否符合條件。

設 $A$ 為特定字元 `c` 是否出現於 $S$，
若 $S_i=$ `c` 則 $A_i = 1$，反之 $S_i\neq$ `c` 時 $A_i = 0$
則區間 $(l, r)$ 裡 `c` 的出現次數可表達為 $A_l + A_{l+1} + \ldots + A_{r-1} + A_r$
如此，特定字元於區間內出現次數，可轉換為區間加總問題。

區間加總問題可透過定義前綴和 $P_i = A_1 + A_2 + \ldots + A_i$
將 $(l, r)$ 區間加總表達為 $P_r - P_{l-1}$
而 $P$ 可藉由 $P_i = P_{i-1} + A_i$ 算出，只需 $n$ 的計算量，
之後所有區間加總只要 $1$ 次計算。

回到前面，窮舉 $l$，對每個 $l$ 用二分搜尋尋找邊界 $k$，
每次查詢區間是否符合條件，需要對每個字元計算區間總和來找出現次數。

計算量為 $n\times \log n\times 100$，可以得到 $80$ 分。

