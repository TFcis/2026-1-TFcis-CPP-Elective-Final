// Always outputs n. WA whenever a strictly-shorter window suffices.
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n, m;
	cin >> n >> m;
	string S, T;
	cin >> S >> T;
	cout << n << "\n";
	return 0;
}
