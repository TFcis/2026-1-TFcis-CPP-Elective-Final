// Always outputs m. WA on basically every test where the optimal window
// strictly exceeds |T| (which is the typical case).
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n, m;
	cin >> n >> m;
	string S, T;
	cin >> S >> T;
	cout << m << "\n";
	return 0;
}
